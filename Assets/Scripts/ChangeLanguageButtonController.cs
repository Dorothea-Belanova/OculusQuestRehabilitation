using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeLanguageButtonController : MonoBehaviour
{
    [SerializeField] private SegmentedControl languageSegmentedControl;

    private void Awake()
    {
        //languageSegmentedControl.OnValueChanged += OnValueChanged;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void CreateSegmentedControl()
    {

    }
}
